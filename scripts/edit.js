var React = require('react');
var ReactDOM = require('react-dom');
var classes = require('./classes.js');

var EditBox = classes.EditBox;

ReactDOM.render(
    <EditBox />,
    document.getElementById('content')
);

